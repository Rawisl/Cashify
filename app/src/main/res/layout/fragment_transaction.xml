<?xml version="1.0" encoding="utf-8"?>
<androidx.core.widget.NestedScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fadingEdgeLength="24dp"
    android:fillViewport="true"
    android:fitsSystemWindows="true"
    android:requiresFadingEdge="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <TextView
            style="@style/screen_title"
            android:text="@string/trans_title" />

        <TextView
            style="@style/screen_subtitle"
            android:text="@string/trans_subtitle" />

        <EditText
            android:id="@+id/etSearch"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:background="@drawable/bg_search"
            android:drawableStart="@android:drawable/ic_menu_search"
            android:drawablePadding="12dp"
            android:fontFamily="@font/inter_regular"
            android:hint="@string/action_search"
            android:imeOptions="actionSearch"
            android:paddingHorizontal="16dp"
            android:textSize="14sp" />

        <LinearLayout
            style="@style/filter_tab_container"
            android:layout_marginTop="16dp">

            <TextView
                android:id="@+id/filterAll"
                style="@style/filter_tab_item.Active"
                android:text="@string/tab_all" />

            <TextView
                android:id="@+id/filterIncome"
                style="@style/filter_tab_item.Inactive"
                android:text="@string/tab_income" />

            <TextView
                android:id="@+id/filterExpense"
                style="@style/filter_tab_item.Inactive"
                android:text="@string/tab_expense" />
        </LinearLayout>

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp">

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/rvHistory"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:nestedScrollingEnabled="false"
                android:clipToPadding="false"
                android:overScrollMode="never" />

            <LinearLayout
                android:id="@+id/layoutEmpty"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="center"
                android:orientation="vertical"
                android:padding="32dp"
                android:visibility="gone">

                <ImageView
                    android:layout_width="150dp"
                    android:layout_height="150dp"
                    android:alpha="0.5"
                    android:contentDescription="@string/trans_empty_noti"
                    android:src="@drawable/ic_empty"
                    app:tint="@color/brand_primary" />

                <TextView
                    style="@style/empty_state_title"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="16dp"
                    android:text="@string/trans_empty_noti"
                    android:textAlignment="center" />

                <TextView
                    style="@style/empty_state_description"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp"
                    android:text="@string/trans_empty_desc"
                    android:textAlignment="center" />

            </LinearLayout>
        </FrameLayout>
    </LinearLayout>
</androidx.core.widget.NestedScrollView>